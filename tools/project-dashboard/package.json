{
  "name": "@addone/project-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:data": "node scripts/generate-dashboard-data.mjs",
    "dev": "node scripts/generate-dashboard-data.mjs && vite",
    "build": "node scripts/generate-dashboard-data.mjs && tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/loader.test.mjs"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^7.1.0"
  }
}
