# AddOne Realtime Broker (Local Dev)

This service runs a lightweight local MQTT broker for staging and hardware validation on the same LAN as the test device.

Production should use a managed MQTT broker. This service exists only to validate the realtime transport locally.
