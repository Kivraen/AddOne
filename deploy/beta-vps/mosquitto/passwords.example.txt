# Generate this file on the VPS with:
#   docker run --rm -it -v "$PWD:/work" eclipse-mosquitto:2 \
#     mosquitto_passwd -c /work/passwords.txt addone-beta-gateway
#   docker run --rm -it -v "$PWD:/work" eclipse-mosquitto:2 \
#     mosquitto_passwd /work/passwords.txt device-fleet-beta
#
# Then update deploy/beta-vps/.env to match the chosen usernames and passwords.
